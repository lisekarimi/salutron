[project]
name = "salutron"
version = "0.1.0"
description = "Generate a random salutation with AI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.0",
    "openai>=1.12.0",
    "gunicorn>=21.2.0",
]
